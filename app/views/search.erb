<div class="container">
  <div class="row">
    <div class="col-lg-8 col-sm-8">
      <h1>Search Results</h1>
        <% if @livesets.empty? %>
          <p>No results found. :( </p>
        <% else %>
          <% @livesets.each do |liveset| %>
            <p>
              <a href="liveset/<%= liveset.id %>">
              <%= liveset.artist %> - <%= liveset.title %> - <%= liveset.date_aired %></a>
            </p>
          <% end %>
          <%= will_paginate @livesets %>
        <% end %>
      </p>
    </div>
  </div>
</div>
